apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "unimus.fullname" . }}-test-connection"
  labels:
    {{- include "unimus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "unimus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
